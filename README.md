# mock-api-server-generator-reads-openapi-be62

A mock API server generator that reads an OpenAPI spec and spins up a local server returning realistic fake...

## Quick Start

```bash
./scripts/run.sh demo-target
```

## Prerequisites

- Bash 3.2+ (macOS compatible)
